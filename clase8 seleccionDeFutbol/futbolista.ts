import {Persona} from "./persona";

class Futbolista extends Persona {
    posicion: number;
    constructor(name:string, profesion: string, posicion: number) {
super (name, profesion);
this.posicion=posicion;

private posicion: string;
private pieHabil: string;
private numero: number;
privare goles: number;
constructor(name: string; equipo: string; edad: number; antiguedad: number, sueldo: number, posiscion: number);